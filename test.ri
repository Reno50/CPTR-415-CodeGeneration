fn func(n: i32) -> i32 { 
    return n > 7;
}

fn main() {
    let mut x: i32;
    let mut y: i32;

    y = -3;
    x = -4;
    println(func(x));
    println(func(y));
    println(func(x) == func(y));
    println(0 == 0);
}